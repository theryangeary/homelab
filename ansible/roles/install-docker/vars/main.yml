---
# Docker uses different architecture names than system reports
docker_arch_map:
  x86_64: amd64
  aarch64: arm64
  armv7l: armhf
  armv6l: armhf

ghcr_token: "{{ lookup('bitwarden.secrets.lookup', 'a11c7900-ee0b-4c0d-8a38-b3ad00209876') }}"
