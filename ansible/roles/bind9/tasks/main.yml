---
- name: Install BIND9.
  ansible.builtin.apt:
    name:
      - bind9
      - bind9utils
      - bind9-doc
    state: present
  become: true

- name: Deploy BIND9 main configuration.
  ansible.builtin.template:
    src: "templates/named.conf.j2"
    dest: "/etc/bind/named.conf"
    owner: bind
    group: bind
    mode: 0644
  notify:
    - Check bind9 config
    - Restart bind9
  become: true

- name: Deploy BIND9 zone files per domain.
  ansible.builtin.template:
    src: "templates/home.arpa.zone.j2"
    dest: "/etc/bind/home.arpa.zone"
    owner: bind
    group: bind
    mode: 0644
  notify:
    - Check bind9 config
    - Restart bind9
  become: true

- name: Ensure BIND9 is running and enabled.
  ansible.builtin.service:
    name: bind9
    state: started
    enabled: true
  become: true

- name: Run handlers before testing to ensure any configuration changes are applied.
  ansible.builtin.meta: flush_handlers

- name: Test that truenas is resolvable from the bind9 server.
  ansible.builtin.command: "dig +short @{{ bind9_ip }} truenas.home.arpa"
  register: dig_truenas
  changed_when: false

- name: Assert that truenas resolves to the correct IP.
  ansible.builtin.assert:
    that:
      - dig_truenas.stdout == truenas_ip
    fail_msg: "DNS resolution for truenas.home.arpa did not return the expected IP address."

- name: Test that jellyfin is resolvable from the bind9 server.
  ansible.builtin.command: "dig +short @{{ bind9_ip }} jellyfin.home.arpa"
  register: dig_jellyfin
  changed_when: false

- name: Assert that jellyfin resolves to the correct IP.
  ansible.builtin.assert:
    that:
      - dig_jellyfin.stdout == jellyfin_ip
    fail_msg: "DNS resolution for jellyfin.home.arpa did not return the expected IP address."

- name: Test that gl is resolvable from (through) the bind9 server.
  ansible.builtin.shell: "dig +short @{{ bind9_ip }} gl.{{ domain_name }} | head -n 1"
  register: dig_gl
  changed_when: false

- name: Assert that gl resolves to the correct IP.
  ansible.builtin.assert:
    that:
      - dig_gl.stdout == "172.67.163.236"
    fail_msg: "DNS resolution for gl.{{ domain_name }} did not return the expected IP address. This public IP address may change from time to time, but as long as it resolves to some public ip address and gl is reachable, it is likely not an issue and this test may need updating. (Returned: {{ dig_gl.stdout }})"
